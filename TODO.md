Вид архитектуры?
Слоистая архитектура: 
UI - 
Logic - 
Data - 
Controler? - 

Что нужно сделать? 

Я создам страничку с главной библиотекой. Там будут лежать карточки книг со всей информацией. Можно будет добавить свою карточку с книгой по кнопке. Должно появляться окошко, в котором нужно будет ввести несколько обязательных данных и несколько не обязательных пунктов. 
По клику на карточку должна открываться отдельная страница, где будет находится текст, который можно будет листать и настраивать. 

Минимум должнен быть такой, есть готовые книги без текста с условным количеством страниц. Можно добавить свои и удалить их.


// Дальше объяснение для меня: 

Что я понял из текста?
1. Должен быть массив, который содержит обьекты, каждый из которых будет книгой!
2. Нужно функция, которая будет брать определённые параметры и на их основе создавать книгу(объект) и сразу помещать новый объект в массив книг.
3. У всех книг должен быть собственный сильный id через crypto.randomUUID()
4. Использовать скелет из примера:

const myLibrary = [];
function Book() {
  // the constructor...
}
function addBookToLibrary() {
  // take params, create a book then store it in the array
}
5. Дальше нужно будет написать функцию, которая проходится по массиву и показывает каждую книгу на странице(с помощью таблицы или карточки)
6. Важная приписка, что код отображения книг и их создания должны быть разделены.
7. Добавить кнопку создания новой книги, там нужно будет заполнить поля: Автор, название, количество страниц, состояние прочитанности(да
нет) и по желанию что угодно ещё.
8. При этом в подверждении данных может помочь функция event.preventDefault().
9.Каждой книги нужна кнопка удалить(скорее всего через id).
10.Для каждой книги добавить кнопку смены статуса прочитанности.


11* Доп. задание от меня.
 11.1 Указано, что сохранять ничего не требуется, но я хочу сделать сохранение через локал сторэдж.
 11.2 Плюс обязательно проверку введенных данных и фильтрация их. 
 11.3 Создать отдельную функцию, которая будет обрабатывать количество символов и выдавать на её основе количество страниц, при чём оно будет учитывать "толстые" символы, пробелы, толщину шрифта.
 11.4 Добавить отдельную страничку с текстами книг. Будет отличной практикой, если я смогу делать пометки закладки и настраивать шрифт.